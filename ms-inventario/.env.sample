# Database connection (inside docker use service name 'postgres', from host use localhost)
DATABASE_URL=postgresql+psycopg2://lux:luxpass@postgres:5432/erp
# If running FastAPI on host and connecting to container postgres:
# DATABASE_URL=postgresql+psycopg2://lux:luxpass@localhost:5432/erp
